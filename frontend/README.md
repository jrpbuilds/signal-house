# Signal House — Frontend

This is the Next.js dashboard application for Signal House.

The full project README is at the [project root](../README.md).

For local development, run:

```bash
npm run dev
```

To build for production:

```bash
npm run build
npm run start -- --hostname 0.0.0.0 --port 8999
```
